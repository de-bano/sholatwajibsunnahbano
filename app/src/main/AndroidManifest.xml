<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sholatwajibsunnah">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.example.sholatwajibsunnah.CardDeskripsi"></activity>
        <activity android:name="com.example.sholatwajibsunnah.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".MenuUtama"
            android:label="@string/shalatwsw" />

        <activity android:name=".MasjidActivity"
            android:label="@string/namamasjid"/>
        <activity android:name=".WajibShalat"
            android:label="@string/wajibshalat"/>
        <activity android:name=".SunnahShalat"
            android:label="@string/sunnahshalat"/>
        <activity android:name=".WudhuActivity"
            android:label="@string/carawudhu"/>
        
        <activity android:name=".ShalatSubuh"
            android:label="@string/shalatsubuh"/>
        <activity android:name=".ShalatDzuhur"
            android:label="@string/shalatdzuhur"/>
        <activity android:name=".ShalatAshar"
            android:label="@string/shalatashar"/>
        <activity android:name=".ShalatMagrib"
            android:label="@string/shalatmagrib"/>
        <activity android:name=".ShalatIsya"
            android:label="@string/shalatisya"/>
        
        <activity android:name=".SsRawatib"
            android:label="@string/sunnahrawatib"/>
        <activity android:name=".SsDhuha"
            android:label="@string/sunnahdhuha"/>
        <activity android:name=".SsTasbih"
            android:label="@string/sunnahtasbih"/>
        <activity android:name=".SsTahajud"
            android:label="@string/sunnahtahajud"/>
        <activity android:name=".SsIstikharah"
            android:label="@string/sunnahistikharah"/>
        <activity android:name=".SsGerhana"
            android:label="@string/sunnahgerhana"/>
        <activity android:name=".SsTaubat"
            android:label="@string/sunnahtaubat"/>
        <activity android:name=".SsHajat"
            android:label="@string/sunnahhajat"/>
        <activity android:name=".SsIstisqa"
            android:label="@string/sunnahistiqa"/>

        <activity android:name=".WudhuInformasi"
            android:label="@string/wudhuinformasi"/>

        <activity android:name=".GerakanSW"
            android:label="@string/gerakanshalat"/>
        <activity android:name=".GerakanSS"
            android:label="@string/gerakanshalat"/>
        <activity android:name=".GerakanWudhu"
            android:label="@string/gerakanwudhu"/>
        
        <activity android:name=".Kentang"
            android:label="@string/about"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

    </application>

</manifest>